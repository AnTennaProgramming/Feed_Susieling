function growth() { let img = document.getElementById("Susieling");
  let currentHeight = img.clientHeight;
            let currentWidth = img.clientWidth;
  img.style.width= (currentWidth + 1) + "px";
img.style.height= (currentHeight + 1) + "px";}